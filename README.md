# Protein-Sequence-ML
The notebook implements dimensional reduction (PCA), clustering, classification (logistic regression), and sequence generation (pseudolikelihood maximization with Gibbs sampling) on real and artificial protein multiple sequence alignment data.
